#########################################本地开发环境#########################################
spring:
  application:
  name: "redisclientdemo"
  http:
    encoding:
      charset: "UTF-8"
      enabled: true
      force: true
  profiles:
    active: cluster
server:
  port: 8081
  servlet:
    context-path: /

logging:
  config: "classpath:logback.xml"


